/**
 * desurf-sdk — embed Desurf contracts in custom runners (Nx, scripts, tests).
 */
export { runSuite, runCase, makeFingerprint, checkDrift, evaluateAssertion, evaluateAll, containPath, sha256, VERSION, ECOSYSTEM, } from "desurf-core";
