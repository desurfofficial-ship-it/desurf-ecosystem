/**
 * desurf-sdk — embed Desurf contracts in custom runners (Nx, scripts, tests).
 */
export { runSuite, runCase, makeFingerprint, checkDrift, evaluateAssertion, evaluateAll, containPath, sha256, VERSION, ECOSYSTEM, type Suite, type TestCase, type SuiteResult, type CaseResult, type Assertion, type Fingerprint, } from "desurf-core";
